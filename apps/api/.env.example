# Local PostgreSQL — copy to `.env` in this directory (`apps/api/.env`).
# `prisma migrate` / `db push` need a reachable database.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/cansell?schema=public"

# API bind (optional)
# PORT=3001
# HOST=0.0.0.0
